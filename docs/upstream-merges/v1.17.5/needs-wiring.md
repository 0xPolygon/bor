# Needs-wiring / deferred backlog: go-ethereum v1.17.5 sync

Durable backlog of upstream changes intentionally **not adopted** during the
sync — features deferred, tests dropped as coverage gaps, adapters skipped.
Nothing is dropped silently: if it is not adopted, it is recorded here.
Fork/EIP enable-decisions live in `fork-register.md`; this file is for the
surrounding *wiring work* a future adoption would require.

`status` ∈ {`deferred`, `coverage-gap`, `adopted`, `wontfix`}.

| Item | source (batch / boundary) | status | why deferred | to adopt (wiring required) | pointers |
| ---- | ------------------------- | ------ | ------------ | -------------------------- | -------- |
| Era store `ere` file support (#34978) | batch 1 (`04bf04530`, `023a57373`) | deferred | The commit adds `execdb`-backed ere files to the era store: `eradb.go` gains a `slim` field, `era.Era` moves from pointer to value, `openEraFile` returns three values, and the file imports `github.com/ethereum/go-ethereum/internal/era/execdb`. **Bor deleted that package entirely** — `internal/era/execdb/{reader,iterator}.go` were kept deleted in this same batch, and the directory does not exist — so the import has no landing site. A per-hunk "keep ours" left `eradb.go` inconsistent with its own auto-merged parts (`entry.slim` undefined, arity mismatches); the coherent form is declining the commit wholesale for this file. Extends the EraE deferral from batch 11 of the v1.17.4 sync. | Restore `internal/era/execdb` (reader + iterator) first, then re-take `core/rawdb/eradb/{eradb,eradb_test}.go` from upstream: add the `slim` field to `fileCacheEntry`, switch `era.Era` to a value type, adopt the 3-value `openEraFile`, and update `fileOpened`'s signature. Gated on bor wanting ere/EraE at all. | upstream boundary `04bf04530`; commit `023a57373`; `core/rawdb/eradb/eradb.go`, `internal/era/execdb/` (absent) |
| `core/eip8037_test.go` (EIP-8037 spec-change tests) | batch 1 (`04bf04530`, `aec597d7b`) | coverage-gap | New upstream test file; references `balChainConfig`, which is defined in `core/bal_test.go` — the file bor keeps deleted under the BAL construction cluster declined at batch 28 of the v1.17.4 sync. Cannot compile against bor. | Only meaningful if bor adopts the BAL construction cluster: restore `core/bal_test.go` (or define `balChainConfig` locally against bor's block-based config), then re-add this file. | removed file, recoverable from upstream `aec597d7b`; relates to the BAL rows in the v1.17.4 `needs-wiring.md` |
| `core/vm/eip8037_test.go` (EVM-level EIP-8037 tests) | batch 1 (`04bf04530`, `aec597d7b`) | coverage-gap | New upstream test file; sets `cfg.AmsterdamTime`, a **timestamp-based** field bor removed — bor gates Amsterdam by block number. Cannot compile. | Re-add and convert the fixtures to bor's block-based config (`AmsterdamBlock` / `IsAmsterdam(num)`); the assertions themselves are fork-agnostic and should port unchanged. Worth doing before Amsterdam is enabled, since bor adopted EIP-8037 in full and currently has no upstream test coverage for it. | removed file, recoverable from upstream `aec597d7b` |
| `core/eip2780_test.go` (EIP-2780 tests) | batch 1 (`04bf04530`, `07dd8e052`) | coverage-gap | New upstream test file; uses `rules8037`, defined in `core/eip8037_test.go`, which is itself removed above. Orphaned by the same decline chain. | Re-add together with `core/eip8037_test.go`; no independent wiring of its own. | removed file, recoverable from upstream `07dd8e052` |
| `core/eip8246_test.go` (EIP-8246 tests) | batch 1 (`04bf04530`, `68671a453`) | coverage-gap | New upstream test file; sets `config.AmsterdamTime`. Same timestamp-vs-block mismatch as `core/vm/eip8037_test.go`. | Re-add and convert to bor's block-based Amsterdam gate. Valuable before enablement: EIP-8246 changes SELFDESTRUCT balance handling and removed bor's dormant EIP-7708 burn-log path, and bor now has no test pinning either behaviour. | removed file, recoverable from upstream `68671a453` |
| `eth/downloader` rename — permanent modify/delete cost | batch 1 (`04bf04530`), recurring | wontfix | Bor renamed `eth/downloader/downloader.go` to `bor_downloader.go`, so every upstream change to `downloader.go` arrives as a modify/delete conflict, on every sync, forever. This batch's instance (#35190) was **not** a pure keep-deleted: the deleted file carried the `GetCanonicalHash` interface declaration that auto-merged call sites in `beaconsync.go` depended on. | Nothing to adopt — but the standing rule matters: **a modify/delete on `eth/downloader/downloader.go` requires a landing-site check in `bor_downloader.go`, never a reflex keep-deleted.** A missed declaration breaks the build; a missed behavioural change is worse, because it compiles. | `eth/downloader/bor_downloader.go`; this batch's instance is commit `12241bde2` (#35190) |
| pebble v1 databases lose bor's amplification metrics | batch 1 (`04bf04530`, `7f3afa1b7`) | deferred | Upstream's pebble v2 support keeps the old implementation as `ethdb/pebble/pebble_v1.go`, selected by on-disk format version. Bor's instrumentation (`#2170` write-path tuning, the `readAmp`/`writeAmp`/`spaceAmp`/WAL gauges) lives in `pebble.go`, which is now the **v2** implementation only. A node whose database predates the v2-capable format therefore reports upstream's basic metrics only until it migrates. Deliberately not duplicated into `pebble_v1.go`, which would fork the tuning across two implementations for a transitional path. | If metric continuity during migration matters: port the gauge set and `#2170`'s write-path tuning into `pebble_v1.go` as well, accepting the duplication until v1 support is dropped upstream. Otherwise close this once the fleet has migrated. | `ethdb/pebble/{pebble,pebble_v1,version}.go`; commit `7f3afa1b7` |
| Zero-`from` reaches intrinsic-gas calculation on the unprotected-tx path | batch 1 (`04bf04530`, `aec597d7b`) | deferred | `core/txpool/validation.go` keeps bor's `if err != nil && !opts.AllowUnprotectedTxs` allowance, so a sender-recovery failure is tolerated when unprotected transactions are permitted. Upstream now threads `from` into `core.IntrinsicGas` and `core.FloorDataGas`, so on that path a zero address feeds gas calculation. The tolerance is pre-existing bor behaviour; this batch widened its blast radius. | Decide whether `AllowUnprotectedTxs` should still bypass a `types.Sender` failure now that `from` is load-bearing. Most likely fix is to fail closed when recovery errors regardless of the flag, since a transaction whose sender cannot be recovered is unusable downstream anyway — but that is a behaviour change and needs its own review. | `core/txpool/validation.go`; commit `aec597d7b` |
| Restore `core/eip8037_test.go` + `core/vm/eip8037_test.go` — unblocks seven dropped EIP test files **(blocked on #35318 — do that first)** | batch 2 (`f9417bb27`), covering batch 1 + batch 2 drops | deferred | **Recommended follow-up, recorded as one row rather than seven.** Batches 1 and 2 dropped seven upstream EIP test files as coverage gaps. Five of them are not blocked on anything of their own — they are orphaned because they reference helpers (`mkState`, `rules8037`, `amsterdam8037EVM`) defined in the two `eip8037_test.go` files. Reading those two shows each is blocked on exactly **one line**: `core/eip8037_test.go:40` calls `balChainConfig()` from the declined `core/bal_test.go`, and `core/vm/eip8037_test.go:47` sets `cfg.AmsterdamTime`. Deliberately not done inside a merge batch — it means re-adding 1359 lines of upstream test code and then reconciling whatever disagrees with bor's diverged EVM, which is a review-worthy change, not a conflict resolution. | Own PR, stacked on the milestone, **after the #35318 adoption row below** — that commit rewrote these tests, so the versions at the v1.17.5 tip target the post-#35318 shape. Re-verified at that tip: the blockers are still one line each, in a now-970-line file rather than the 614 assessed here. (1) Re-add `core/eip8037_test.go`, replacing `balChainConfig()` with a local three-line helper: copy `params.MergedTestChainConfig`, set `AmsterdamBlock = new(big.Int)`, return it. (2) Re-add `core/vm/eip8037_test.go`, converting `cfg.AmsterdamTime = new(uint64)` to `cfg.AmsterdamBlock = new(big.Int)`. (3) Re-add the five orphans unchanged and run them. Payoff is real: bor adopted EIP-8037, EIP-8038, EIP-2780, EIP-7997 and EIP-8246 in full and currently has **no** upstream test coverage for any of them, all of which activate together at Amsterdam. Worth landing before Amsterdam is enabled. | removed files recoverable from upstream `f9417bb27`; supersedes the four batch-1 coverage-gap rows above and the three below |
| `core/eip7997_test.go` (EIP-7997 tests) | batch 2 (`f9417bb27`, `c8953d10c`) | coverage-gap | New upstream test file; calls `mkState`, defined in `core/eip8037_test.go` which batch 1 removed. No dependency of its own on `AmsterdamTime` or `balChainConfig`. | Covered by the unblock row above; nothing file-specific needed. | removed file, recoverable from upstream `c8953d10c` |
| `core/eip8038_test.go` (EIP-8038 state-transition tests) | batch 2 (`f9417bb27`, `dd8dd1520`) | coverage-gap | New upstream test file; uses `mkState` and `rules8037`, both from `core/eip8037_test.go`. | Covered by the unblock row above. | removed file, recoverable from upstream `dd8dd1520` |
| `core/vm/eip8038_test.go` (EIP-8038 EVM-level tests) | batch 2 (`f9417bb27`, `dd8dd1520`) | coverage-gap | New upstream test file; uses `amsterdam8037EVM`, defined in `core/vm/eip8037_test.go` which batch 1 removed. Would be the only coverage of `gasSLoad8038`, which bor now carries as live (dormant) code. | Covered by the unblock row above. | removed file, recoverable from upstream `dd8dd1520` |
| `--override.amsterdam` is absent from bor's own CLI | batch 2 (`f9417bb27`, `7e625dd54`) | deferred | #35213's flag was adopted into the **geth-inherited** CLI (`cmd/geth`), which is where upstream put it. Bor's production entrypoint is `internal/cli/server` (`bor server --config ...`), which has its own flag surface and does not expose the override flags at all — the same twin-CLI split already recorded for the cgroup memory-sizing issue. So an operator running bor the normal way cannot use `--override.amsterdam`, `--override.osaka` or `--override.verkle`. | Decide whether bor's own CLI should expose fork overrides. If yes, add them to `internal/cli/server`'s flag set and thread them into the same `ethconfig.Config` fields; the plumbing below `ethconfig` is already shared, so this is CLI surface only. Pre-existing for the other two overrides; this batch added a third instance rather than creating the problem. | `cmd/geth/{chaincmd,config}.go`, `cmd/utils/flags.go`, `internal/cli/server/` |
| `cmd/keeper` does not build standalone; `go mod tidy` there is not clean | batch 2 (`f9417bb27`), pre-existing | deferred | `go mod tidy` in `cmd/keeper` adds 25 `/go.mod` hash lines to `cmd/keeper/go.sum`, and without them `go build ./...` inside that module fails with `missing go.sum entry for go.mod file` (`0xPolygon/crand`, `consensys/gnark-crypto`, others). Verified identical at the batch-1 tip `300b300aa`, so it predates this batch; batch 1's verification note claiming tidy was clean there is corrected in `ledger.md`. No commit in `04bf04530..f9417bb27` touches `cmd/keeper`, so the churn was reverted rather than folded into a merge commit. | Run `go mod tidy` in `cmd/keeper` and commit the resulting `go.sum` as its own small change, so the module builds standalone and the diff is reviewable on its own rather than buried in a merge. | `cmd/keeper/go.{mod,sum}` |
| **Align bor's PIP-88 SSTORE gas function with upstream's post-#35261 ordering** — owed on top of this milestone | batch 3 (`76e3dc6b5`, `cf35a1b6f`) | deferred | #35261 restructured upstream's `gasSStore8037And8038` so the slot's access cost is resolved and checked for affordability **before** `GetStateAndCommittedState` is called, rather than reading committed state up front in the `var` block. The motivation is that a cold access cost above `SstoreSentryGasEIP2200` (2300) means the reentrancy sentry alone no longer establishes that the access is affordable. Bor's `makeGasSStoreFuncPIP88` in `core/vm/operations_acl.go` keeps the original ordering, and `ColdSstoreCostPIP88` is 2940 — above the same 2300 sentry — so the same restructuring applies to it. Upstream's own `makeGasSStoreFunc` needs no change: `ColdSloadCostEIP2929` is 2100, below the sentry. **Deliberately not done inside a merge batch.** Unlike upstream's version, which is dormant behind a nil Amsterdam gate, bor's PIP-88 path is enabled by `newChicagoInstructionSet()` and Chicago is active on both production networks, so this is a live EVM gas path and wants its own review, tests and rollout note. | Own PR, stacked on the milestone, once the sync lands. (1) Mirror upstream's ordering in `makeGasSStoreFuncPIP88`: resolve the access-list state and cost first, add the affordability guard, then read committed state. Keeping the two implementations structurally aligned also reduces future merge friction. (2) **Charge nothing new** — no gas cost, refund, or opcode price changes; PIP-88's repricing is untouched. The guard is outcome-neutral by construction: the minimum cold-slot total in that function is 3040 (2940 + `WarmStorageReadCostEIP2929`), above the 2940 threshold, so any call the guard rejects already fails at `UseGas` today, and both paths surface as `ErrOutOfGas` (`core/vm/interpreter.go:276` vs `:280`) consuming the frame's gas identically. (3) Add boundary tests in the 2300–2940 window: `TestPIP88SStoreGas` walks the charged-gas branches only and does not cover it. (4) **Verify the read-set implication before merging** — the change works by not performing a state read on those calls, which alters the transaction's BlockSTM read set and therefore what the generated witness carries. Final state is unchanged, but witness composition is covered by the standing rule and needs explicit sign-off, not inference. (5) Note Erigon parity in the PR: weak here, since an outcome-neutral change cannot diverge, but state it rather than omit it. | `core/vm/operations_acl.go` (`makeGasSStoreFuncPIP88`), `core/vm/gas_table.go` (`gasSStore8037And8038`, upstream's post-fix shape), `params/protocol_params.go`; upstream commit `cf35a1b6f` |
| EIP-7997 is not applied during bor block building | batch 3 (`76e3dc6b5`, `4d2181aa4`) | deferred | #35285 moved the EIP-7997 irregular state transition into `core.PreExecution` so it runs for block building, simulation and tracing rather than block processing only. Bor's miner never calls `core.PreExecution`: `(*worker).prepareWork` inlines EIP-4788 and EIP-2935 as separate steps with their own EVM contexts, and has no EIP-7997 step. The conflicted region in `miner/worker.go` is empty on bor's side, so wiring it would be authoring new consensus-path code rather than resolving a conflict. Inert today — Amsterdam is nil on every preset — but a bor producer building the activation block would omit the factory and disagree on the state root with every validator that inserts it. | Amsterdam enablement work: add the transition to `prepareWork` alongside the existing inlined steps, gated on `IsAmsterdam(header.Number) && !IsAmsterdam(parent.Number)` to match `core.PreExecution`, and cover the activation boundary at N-1/N/N+1. Consider instead making bor's miner call `core.PreExecution` so the two paths cannot drift again. Pairs with the v1.17.4 note that `consensus/bor` does not assert the EIP-7928/7843 header fields. | `miner/worker.go` (`prepareWork`, ~line 2080), `core/state_processor.go` (`PreExecution`); upstream commit `4d2181aa4` |
| `TestBlockAccessListsUnavailableDecode` (eth/71 BAL decode test) | batch 3 (`76e3dc6b5`, `3006c4411`) | coverage-gap | Auto-added by #35286 outside any conflict region, and references `BlockAccessListPacket` plus the `makeTestBAL` helper — both absent, because bor carries no eth/71 BAL surface at all. Removed to keep the package compiling. | Only meaningful if bor adopts eth/71 and the BAL exchange; covered by the existing eth/70+ and BAL deferral rows from the v1.17.4 sync. | removed test, recoverable from upstream `3006c4411`; `eth/protocols/eth/handler_test.go` |
| **EIP-2780 / EIP-8037 spec changes (#35318)** — owed on top of this milestone | batch 4 (`6e6fcef0b`, `1ef0ffb98`) | deferred | **Operator decision: take as its own stacked PR.** Not a spec tweak but a structural rework — `IntrinsicGas` drops `costPerStateByte` and returns `uint64` instead of `vm.GasCosts`, `preCheck` gains a `rules params.Rules` parameter, and the gas-budget reservation moves out of `buyGas`; 572 lines in `core/state_transition.go` alone plus seven `core/vm` files. It **intersects live bor behaviour**: bor's EIP-7825 cap reads `!isAmsterdam && (isOsaka \|\| isMadhugiri)` where upstream's replacement is `!rules.IsAmsterdam && rules.IsOsaka`, dropping `isMadhugiri` — live on both production networks. Entirely Amsterdam-gated, so declining is behaviour-neutral today, but it leaves bor's dormant EIP-8037/2780 at the superseded spec. Full footprint reverted to bor HEAD; #35342 and #35301, which share files with it, were verified independent and re-applied. | Own PR, stacked on the milestone. Port the rework onto bor's diverged `state_transition.go`, preserving: (1) the Madhugiri arm of the EIP-7825 cap; (2) bor's block-based one-arg fork gates; (3) bor's fee-burn/tip locals and state-sync handling in `TransitionDb`. Upstream links four EIP spec diffs in the commit message — review the implementation against those rather than against the diff. Then re-take the dropped EIP test files. **This work must precede that restoration:** #35318 rewrote those tests (+384 lines in `core/eip8037_test.go`, +643 in `core/eip2780_test.go`, +117 in `core/eip8038_test.go`), so upstream's current versions target the post-#35318 shape and restoring them first would mean restoring tests for a shape bor does not have. Taken in this order they become this PR's verification. | upstream commit `1ef0ffb98`; `core/state_transition.go`, `core/vm/{evm,gas_table,gascosts,instructions,interpreter,interface,operations_acl,common}.go`, `core/txpool/validation.go`, `core/tracing/hooks.go` |
| Peer protection from random dropping by tx-inclusion stats (#34702) | batch 4 (`6e6fcef0b`, `111e7b8b4`) | deferred | Adds an `eth/txtracker` package that scores peers by how often their announced transactions get included/finalized, and exempts high-value peers from the dropper's random eviction. Built on upstream's `NewTxFetcher(chain, validateMeta, addTxs, fetchTxs, dropPeer)`; bor's is `NewTxFetcher(hasTx, addTxs, fetchTxs, dropPeer)` because the `chain`/`validateMeta` refactor was declined earlier. A per-file decline stranded the auto-added `eth/txtracker` package plus its references in auto-merged parts of `eth/dropper.go` and `eth/handler.go`, so the full footprint was reverted and the package removed. | Adopt the `chain`/`validateMeta` TxFetcher refactor first, then re-take #34702: restore `eth/txtracker`, add the `onAccepted` callback to bor's fetcher, and wire `dropSkipped` plus the protection fractions into bor's dropper. Operationally attractive for block producers, which want to keep well-behaved peers. | upstream commit `111e7b8b4`; `eth/{dropper,handler,backend}.go`, `eth/fetcher/tx_fetcher.go`, `eth/txtracker/` |
| Engine-API surface in this range: BAL payload bodies v2, RLP fixes, max-reorg-depth knob (#35347, #35348, #35335) | batch 4 (`6e6fcef0b`) | deferred | Declined as one group because all three share `eth/catalyst/api.go`. #35347/#35348 extend `ExecutableData.BlockAccessList`, a field bor removed with the declined BAL construction cluster — keeping only the conflicted halves left `beacon/engine/types.go` referencing `bal` with unused `bytes`/`rlp` imports. #35335 is independent on its merits, but its accessor and enforcement had auto-merged, so retaining just its config plumbing would have shipped an `--engine.maxreorgdepth` flag wired to nothing. Bor does not use the Engine API in production (see `.claude/rules/eth-backend-security.md`), so the group was reverted whole for one coherent story. | If bor ever exercises the Engine API: adopt the BAL cluster first, then re-take #35347/#35348 together, and re-take #35335's knob (config field, flag, `Ethereum.EngineMaxReorgDepth()` accessor, and the `maxReorgDepth` check in `ConsensusAPI`). #35335 can be taken independently of the other two. | upstream commits `b34a925e9`, `68f711b9d`, `abfb2de57`; `beacon/engine/{types,ed_codec}.go`, `eth/catalyst/api.go`, `eth/ethconfig/{config,gen_config}.go`, `cmd/utils/flags.go` |
| eels tests@v20.0.0 harness fixes (#35283) | batch 4 (`6e6fcef0b`, `2ce020076`) | deferred | Updates the test harness for the EELS `tests@v20.0.0` fixture release and repoints `build/checksums.txt`/`build/ci.go` at it. Bor does not consume that fixture release, and bor's `tests/init.go` carries none of upstream's BPO config table, so the conflicted half was not applicable — but declining it alone stranded `tests/gen_stenv.go`, which gained a `SlotNumber` accessor for a field bor's `stEnv` does not have. Full footprint reverted. | Only relevant if bor starts running the EELS blockchain-test fixtures in CI. Would need the `stEnv` `SlotNumber` field (post-Amsterdam) and bor's own fork entries added to the harness config table. | upstream commit `2ce020076`; `tests/{init,gen_stenv,block_test,state_test,state_test_util,transaction_test}.go`, `build/{ci.go,checksums.txt}` |
| Bor's `LatestFork` and startup banner omit Amsterdam | batch 4 (`6e6fcef0b`), pre-existing since batch 1 | deferred | `ChainConfig.LatestFork` tops out at Osaka and never returns `forks.Amsterdam`, and the post-merge banner has no Amsterdam line; `checkCompatible` likewise has no Amsterdam entry. Noticed while placing Bogota, which follows the same shape deliberately rather than being made inconsistent with its neighbour. Inert while both gates are nil, but `ActiveSystemContracts` keys off `LatestFork(time) >= forks.Amsterdam` to report the EIP-8282 builder contracts, so those would never be reported active even once Amsterdam is enabled. | Amsterdam enablement work: add the `LatestFork` cases, banner lines and `checkCompatible` entries for Amsterdam **and** Bogota together, and re-check `ActiveSystemContracts`. Cheap; grouped here so it is not rediscovered per fork. | `params/config.go` (`LatestFork`, `Description`, `checkCompatible`, `ActiveSystemContracts`) |
| **Sparse blobpool / eth/72 (#34047)**, plus #35387 and #35393 | batch 5 (`81ab8b594`, `d91b71fb3`) | deferred | EIP-8070: relays blob **cells** instead of whole blobs over a new **eth/72**, adds `BlobBuffer`/`BlobFetcher`, a consensus-supplied custody bitmap via `forkchoiceUpdatedV4`, and `engine_getBlobsV4`. 47 files, the largest commit in the range. Declined on two independent grounds: bor is on `[ETH69, ETH68]` having declined eth/70 (#33153) and eth/71, so eth/72 cannot precede them; and `core/txpool/blobpool/cache.go`, which this extends by 231 lines, does not exist in bor. **Fourth deferral on the eth-protocol surface** — the divergence is compounding and deserves one deliberate team decision rather than another batch-by-batch deferral. Two commits fall with it because their only files are ones it creates: #35387 (`conversion{,_test}.go`) and #35393 (`blob_fetcher.go`). | Sequenced behind the eth/70 and eth/71 decisions; see the #33153 row in the v1.17.4 backlog for the `ExcludeStateSyncReceipt` design question that gates them. Once those land: restore `blobpool/cache.go`, then take #34047's `buffer.go`/`conversion.go`/`custody_bitmap.go`/`blob_fetcher.go`, extend `ProtocolVersions`, and wire the custody bitmap through bor's engine-API surface (itself declined). Only meaningful if Polygon carries blob traffic worth cell-level relay. | upstream commit `d91b71fb3`; 47 files, listed in the batch-5 ledger entry |
| `core/eip7708_test.go` (EIP-7708 fork-coverage test) | batch 5 (`81ab8b594`, `6e49f8e6b`) | coverage-gap | Added by #35364 outside any conflict; references `senderKey`, defined in `core/eip8037_test.go` which batch 1 removed. Same decline chain as the other orphans — nothing wrong with the test itself. | Covered by the `eip8037_test.go` restoration row, which is itself blocked on #35318. | removed file, recoverable from upstream `6e49f8e6b` |
| Engine-API and GOGC commits declined in batch 5 (#35372, #35373, #35369, #35378, #35396, #35363, #34851) | batch 5 (`81ab8b594`) | deferred | Grouped for brevity; each falls out of an earlier decision rather than being a new judgement. #35372 (`TargetGasLimit` via engine API) and #35373 (preserve zero reorg depth) belong to the engine-API group declined in batch 4. #35369 adds a block access list to the chain export format, from the declined BAL cluster. #35396 edits `initRuntimeGasBudget`, introduced by the deferred #35318. #35378 has no landing site — bor's `BlockChain` carries no `jumpDestCache`. #35363/#34851 are **twins bor already implements**: a `cache.gogc` flag in `internal/cli/server/` and the cache-derived heuristic at `cmd/utils/flags.go:1797`. | Re-evaluate #35372/#35373 with the engine-API group, #35369 with the BAL cluster, and #35396 alongside #35318. #35378 becomes available if bor ever adds a chain-level jumpdest cache. The GOGC pair needs nothing unless bor consolidates its two CLIs. | upstream commits as listed; footprints in the batch-5 ledger entry |
| **State-prefetcher / block-processing coordination (#35404)** | batch 6 (`9621c6ad1`, `ff44a796a`) | deferred | Upstream threads an `execIndex *atomic.Int64` through `Prefetcher.Prefetch` and `Processor.Process` so prefetch workers skip transactions the main pass has already reached, and promotes >1M-gas transactions to the front of the prefetch queue. Declined because bor cannot host it as written: both interfaces are diverged in both directions (`author`, `interruptCtx`, `*PrefetchResult`, no leading `ctx`, no `jumpDestCache`); a scalar exec index has no sound meaning under BlockSTM, which executes out of order and speculatively, so publishing one only from the serial processor would give a silently wrong signal on the production path; and bor's `PrefetchStream` workers carry arrival-order indices rather than block positions, with a second caller that streams transactions with no block to sort. Prefetch-adjacent and touches `core/blockchain.go`, so surfaced rather than resolved. Efficiency, not correctness. | Decide first whether the parallel processor can publish a meaningful watermark (e.g. lowest un-committed index in BlockSTM's scheduler) — without that the rest is not worth porting. Then carry the block index alongside each transaction on the prefetch channel so a worker can compare, keep `prefetchOrder` to the block-oriented `Prefetch` wrapper only (it is meaningless for the streaming caller), and add `blockPrefetchTxsSkippedMeter`. Re-check against the witness rule before touching `core/blockchain.go`. | upstream commit `ff44a796a`; `core/{blockchain,state_prefetcher,state_processor,stateless,types}.go`, `eth/state_accessor.go` |
| Bor's `maxReorgDepth` check rejects a reorg of exactly the limit | batch 6 (`9621c6ad1`, `b2ee83931`) | deferred | Upstream's #35391 changed `depth >= api.maxReorgDepth` to `depth >` so a depth equal to the configured limit is accepted. It was re-declined here with the rest of the Engine-API group, but the **bor-only twin has the same off-by-one**: `eth/catalyst/api.go` has a package-level `maxReorgDepth = 32` documented as *the maximum reorg depth accepted*, checked with `depth >= maxReorgDepth`, so a 32-deep reorg is refused. Not urgent — bor does not use the Engine API in production (`.claude/rules/eth-backend-security.md`) — but it is a real inconsistency between the constant's documented meaning and its use. | One-character change (`>=` → `>`) plus a boundary test at depth 31/32/33. Worth doing together with any future adoption of #35335's configurable `EngineMaxReorgDepth`, which would replace the constant outright. Flagged separately because it changes accept/reject behaviour on a bor-authored constant and is a bor decision, not an upstream-merge one. | `eth/catalyst/api.go:86` (const), `:377` (check) |
| Blob-pool transaction size calculation (#35406, blobpool half) | batch 6 (`9621c6ad1`, `ca1f2e4d3`) | deferred | Upstream rewrote `txSize`/`txSizeWithoutBlob` to assume the v1 cell-proof wire form, since its pool no longer accepts v0 sidecars. Written entirely against `BlobTxForPool.CellSidecar`, a shape bor does not have — bor's `blobTxForPool` still holds plain `Blobs`, having declined #35191 (v0 sidecars) and #34047 (cell sidecars / eth/72). The companion `core/types/tx_blob.go` fix **was** adopted, being version-conditional and correct for both shapes. | Falls out of the #34047 / eth-protocol decision. If bor ever adopts cell sidecars, take this with it; until then bor's pool holds v0 and the v0 formula is the right one. | upstream commit `ca1f2e4d3`; `core/txpool/blobpool/blobpool.go` |
